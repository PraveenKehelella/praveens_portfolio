OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_TOKENS=220

# Compose default on the VPS is 127.0.0.1:8040 (avoids production-pulse on :8000).
# For local Docker on :8000, set HOST_PORT=8000.
HOST_BIND=127.0.0.1
HOST_PORT=8040
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ORIGINS=*
